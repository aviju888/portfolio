# Portfolio Project Notes

## Deployment

- **Vercel deploys from the `nextjs` branch, NOT `main`**
- When pushing changes to deploy, always use: `git push origin HEAD:nextjs`
- If changes aren't showing on avijuan.com after push, check which branch was pushed to
